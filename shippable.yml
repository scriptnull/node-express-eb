# Language setting
language: node_js

# Version number
node_js:
  - "5.3"

build:
  ci:
    - node --version

  post_ci:
    - echo "post_ci"
    # - cd /root/src/github.com/harryi3t/node-express-eb
    # - eb deploy

integrations:
  deploy:
    - type: aws
      integrationName: aws-test
      target: eb_paas
      application_name: sample-app-vb-2
      env_name: not-present
      region: us-east-1
